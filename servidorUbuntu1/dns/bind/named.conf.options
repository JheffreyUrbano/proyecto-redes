options {
    directory "/var/cache/bind";

    forwarders {
        8.8.8.8;
        8.8.4.4;
    };

    forward only;
    recursion yes;
    allow-recursion { any; };
    allow-query { any; };

    dnssec-validation no;
    listen-on { any; };
};
